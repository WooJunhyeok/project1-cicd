global:
  resolve_timeout: 5m

route:
  receiver: slack

receivers:
  - name: slack
    slack_configs:
      - send_resolved: true
        api_url: "{{ slack_webhook_url }}"
        channel: "{{ slack_channel }}"

